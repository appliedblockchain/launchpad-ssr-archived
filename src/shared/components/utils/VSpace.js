import React, { Component } from 'react'
import Nav from '../nav/Nav'

class VSpace extends Component {
  render() {
    return (
      <div className={this.props.s} />
    )
  }
}

export default VSpace
